**Few-Shot Example: Refactor**
- Scope: preserve public behavior while changing structure, names, or boundaries.
- Output: keep API compatibility unless the plan explicitly allows a breaking change.
- Avoid: mixing behavior fixes with refactor-only work unless needed to keep tests passing.
