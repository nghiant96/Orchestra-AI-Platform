# Current Project Tasks

Last updated: 2026-06-12

## Task: Implement v0.12 Context Builder And Diff Summarizer

- [x] Add automatic diff summarizer output for `ai diff explain`.
- [x] Add the context builder, ripgrep candidate search, semantic provider interface, and memory namespace.
- [x] Write pre-context artifacts and wire them into worker and Solo flows.
- [x] Add targeted tests for builder, pre-context artifacts, and setup prompt propagation.
- [x] Update roadmap completion state for v0.12.
- [x] Run targeted tests, typecheck, lint, `git diff --check`, and `check:all`.

Review result:

- v0.11 continue/commit slice passed targeted tests, typecheck, focused lint, `git diff --check`, and full `pnpm check:all`.
- v0.12 diff summarizer and context builder slice passed targeted tests, typecheck, focused lint, `git diff --check`, and full `pnpm check:all`.
